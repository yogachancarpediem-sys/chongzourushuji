# 下周优化待办清单 v2

> 综合自检 + GPT外部视角建议 + Kychnrona《小白教程：如何用AI做一个线上博物馆》方法论启发
> 版本 v=37 → 目标 v=42

---

## 🔴 P0 — ✅ 全部完成（5/5）

### 1. 安静开场 2.0 ✅
**来源**: GPT建议 + 自检
**问题**: 当前开场页元素过多（WebGL shader + 水墨 + 浮动文字粒子 + SVG山形 + 路线图 + 角色卡 + 按钮），虽然大气但不够"安静"
**方案**:
- 保留 WebGL shader 作为底层（已有，降低复杂度）
- 移除水墨 div、浮动粒子、路线图和角色卡（移到点击后展示）
- 开场只保留：标题 + 一行简介 + 一枚印章 + 缓慢呼吸的柔光
- 用户停留 3-5 秒后，"踏上诗旅"按钮以极慢淡入出现
- 参考情绪：Journey / Sky: Children of the Light 的"等待感"
**文件**: `index.html` (opening section), `css/style.css`

### 2. 多层视差滚动（地图视图）
**来源**: GPT建议
**问题**: 地图视图是核心体验，但目前只有雾层效果，缺少空间纵深感
**方案**:
- A 层（远山/云层）: 滚动速度 0.15x，模糊度随深度增加
- B 层（水面/河流）: 滚动速度 0.35x
- C 层（驿站卡片列表）: 正常滚动 1.0x
- D 层（角色气泡 UI）: 固定在 viewport 底部
- 使用 CSS `transform: translate3d` + JS scroll 事件驱动
- 移动端降级为 2 层
**文件**: `js/app.js` (renderMap + scroll handler), `css/style.css` (.map-container)

### 3. 环境音系统
**来源**: GPT建议
**问题**: 当前有 BGM 和 UI 音效（sfx.js），但缺乏场景环境音
**方案**:
- 每个驿站配置环境音类型：临安(雨声)、枫桥(钟声+水声)、巫山(风声+虫鸣)等
- 使用 Web Audio API 合成（参考 sfx.js 的噪声合成），无需外部文件
- **默认关闭**，在地图视图右上角增加环境音开关（🔇/🌿）
- 音量极轻（volume 0.08-0.12），作为"空气"而非"内容"
- 驿站详情页根据当前驿站切换环境音
**文件**: `js/ambient.js` (新建), `js/data.js` (每个驿站加 ambientSound 字段), `index.html`

### 4. 低频惊喜微交互
**来源**: GPT建议
**问题**: 角色动效只在特定事件触发（解锁/答题），缺少"闲时生命感"
**方案**:
- 实现 idleEvents 系统：用户在地图/驿站页面停留 15-30 秒随机触发
- 事件池：
  - 🐱 小猫打喷嚏（猫角色快速 shake + 粒子）
  - 🍂 落叶飘过（CSS animation，随机位置+速度）
  - 🦅 白鹭飞过（SVG 路径动画，屏幕右上→左下）
  - ✨ 诗句文字被"风吹散"（某个 span 的 letter-spacing 渐增后恢复）
  - 🙆 陆小六伸懒腰（角色图短暂缩放 + 旋转）
- 每个事件冷却 30-60 秒，不会频繁打扰
**文件**: `js/idle-events.js` (新建), `css/style.css`, `index.html`

### 5. 感知型文本优化
**来源**: GPT建议
**问题**: 驿站详情首屏展示的是说明性 description，缺乏文学感
**方案**:
- 每个 STATION 新增 `moodText` 字段：一句文学性短句（如"夜雨刚停，池水像一张被风揉皱的纸"）
- 详情页 hero 区：moodText 替代 description 作为首屏主文案（大字、低透明度、居中）
- description 改为折叠区"📖 背景考据"，默认收起，点击展开
- diary 区前加一句引导语（小字灰调）："以下为《入蜀记》原文，陆游亲笔所记——"
**文件**: `js/data.js` (新增 moodText), `js/app.js` (openStation 渲染), `css/style.css`

---

## 🟡 P1 — ✅ 全部完成（3/3）

### 6. 动画系统统一 (Design Token) ✅
**方案**:
```css
--ease-out: cubic-bezier(0.23, 1, 0.32, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.15, 1);
--ease-soft: cubic-bezier(0.4, 0, 0.2, 1);
--dur-fast: 0.2s; --dur-normal: 0.3s; --dur-slow: 0.55s;
--dur-slide: 0.8s; --dur-float: 6s; --dur-breathe: 4s;
```
- 全局替换 `cubic-bezier(0.23, ...)` → `var(--ease-out)` (8处)
- 全局替换 `cubic-bezier(0.4, 0, 0.15, 1)` → `var(--ease-in-out)` (2处)
- 全局替换 `cubic-bezier(0.4, 0, 0.2, 1)` → `var(--ease-soft)` (2处)
- 过渡时长：`0.3s` → `var(--dur-normal)`、`0.2s` → `var(--dur-fast)`、`0.25s` → `var(--dur-fast)`、`0.4s` → `var(--dur-slow)`

### 7. 视觉层级显式化 ✅
```css
--z-bg: 0;      --z-scene: 10;    --z-float: 20;
--z-overlay: 50; --z-ui: 100;      --z-modal: 200;  --z-toast: 300;
```
- 全局替换硬编码 z-index → Token

### 8. 沉浸阅读模式 ✅
- 驿站详情 hero 区新增"🌙 沉浸阅读"按钮
- 点击后：隐藏 hero 文本/风物志/古今对照/碎片/角色区，只保留日记+诗歌+环境音
- 背景变为纯色 parchment，字体加大（diary 1.05rem, poem 1.1rem），行距增加
- 再次点击或离开驿站自动退出

---

## 🟢 P2 — 锦上添花，时间允许再做

### 9. 「蜀道总览」分析页（策展结构补全）
**来源**: 外部文章启发 — 数字策展需要五种页面类型：入口页/索引页/专题页/分析页/资料页。当前项目缺"分析页"。
**内容**:
- 情绪旅程图：按 mood 字段分组展示9站，配合情绪色板（期待=暖金、离愁=冷蓝、豁达=柔粉），可视化陆游从临安到夔州的心态变化
- 蜀道地形剖面图：纵向海拔 × 驿站位置（mapping 制图语言，非装饰性插画）
- 时间线：乾道六年十月→十二月，关键节点标注（出发/受阻/转道/抵达）
- 此页定位：让观众看完各驿站后，有一个"俯瞰全局"的页面
**文件**: `js/app.js`, `css/style.css`, `index.html`

### 10. 诗句收藏 + 个人印章
**来源**: GPT建议
**方案**: 
- 诗歌卡片增加 ⭐ 收藏按钮，存入 localStorage
- 在诗集视图中增加"我的收藏"tab
- 终章成就卡片自动盖一枚"蜀道行者"电子印章（带用户名/日期）
**文件**: `js/app.js`, `css/style.css`

### 11. 资料层建设：原文出处标注 + 风物志入口升级
**来源**: 外部文章启发 — "让观众能回到依据"
**方案**:
- 驿站详情 diary 区每段末尾标注出处：如"——乾道六年十月三日"
- 风物志入口从"彩蛋按钮"改为正式入口："📖 原文考据"
- 终章新增"参考资料"页：列出《入蜀记》版本来源、参考文献、插画创作说明
**工作量**: 小（主要是 data.js 加字段 + 少量 CSS）
**文件**: `js/data.js`, `js/app.js`, `index.html`, `css/style.css`

### 12. 风景插画策展性图注
**来源**: 外部文章启发 — "一张图只表达一个核心信息，为图配上策展性说明而非装饰性标题"
**方案**:
- 9张驿站风景插画 + 终章罨画池插画，各配一句策展性图注
- 图注不是"某某驿站风景"，而是提炼这张图服务的策展主题
- 例：临安插画 → "这是临安，也是陆游与收复故土的梦想告别的地方"
**工作量**: 极小（纯文案，data.js 加字段）
**文件**: `js/data.js`

### 13. 交互叙事关联标注
**来源**: 外部文章启发 — "交互必须对应策展关系，否则会干扰阅读"
**方案**:
- 驿站详情 hero 区标注当前环境音及其叙事理由（如"乾道六年十月八日，微雨"）
- 角色微动效触发时加一句极轻的叙事 text（如"陆小六走累了……"）
- BGM/环境音切换在 UI 上给观众一个"为什么变了"的交代
**工作量**: 小
**文件**: `js/app.js`, `css/style.css`

---

## ❌ 不采纳或已实现

| 建议 | 原因 |
|------|------|
| 内容与场景解耦（第10条） | ✅ 已实现 — 数据早已在 `js/data.js` 的 STATIONS 数组 |
| 用户留痕/点亮驿站/生成旅程签（第9条） | ✅ 大部分已实现 — visited + fragments + gallery + achievements |
| 统一"空气感"世界观（第4条） | 方向性指导，非具体任务，融入后续设计即可 |
| 参考千与千寻/长安三万里/Journey | 风格参考，不是开发任务 |

---

## 预估工作量

| 优先级 | 条目数 | 内容 | 预估人天 |
|--------|--------|------|----------|
| P0 | 5 | 安静开场/视差/环境音/微交互/感知文本 | ✅ 已完成 |
| P1 | 3 | 动画Token/层级Token/沉浸阅读 | ✅ 已完成 |
| P2 | 5 | 蜀道总览/诗句收藏/资料层/策展图注/叙事标注 | 2-3 天 |
| **合计** | **13** | | **P2 待做** |
